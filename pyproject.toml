[project]
name = "compem"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "seiHU06", email = "r.god.510@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
compem = "compem:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
